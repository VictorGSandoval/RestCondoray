use FOOTIFY;
/*Consultas*/
SELECT IDUBI AS UBICACION,NOMPER AS PERSONA,NOMMEN AS MENU,IDPER AS ID FROM PERSONA,MENU; /*Consulta de la cocinera*/
SELECT CODVEN AS CODIGO_VENTA,FECVEN AS FECHA,NOMPLA AS PLATO,IDVENDET AS ID FROM VENTA,VENTA_DETALLE,PLATO; /*Consulta del administrador*/
SELECT NOMPER AS NOMBRE, SEXPER AS SEXO, CELPER AS NUMERO_TELEFONICO, NOMPLA AS MENU, PREPLA AS PRECIO FROM PERSONA,PLATO; /*Consulta del comensal*/
SELECT NOMPER AS USUARIO, PASPER AS CONTRASEÑA FROM USUARIO; /*Consulta del Usuario*/

/*Vistas*/
CREATE VIEW
VISTA_COMENSAL AS 
SELECT IDUBI,NOMPER,NOMMEN,IDPER FROM PERSONA,MENU; /*Esta vista esta dada por la cocinera*/

select * FROM vista_comensal;

CREATE VIEW
VISTA_ADMIN AS
SELECT CODVEN AS CODIGO_VENTA,FECVEN AS FECHA,NOMPLA AS PLATO,IDVENDET AS ID FROM VENTA,VENTA_DETALLE,PLATO; /*Esta vista esta dada por el Administrador*/

select * FROM vista_admin;

CREATE VIEW
VISTA_PERSONA AS
SELECT NOMPER AS NOMBRE, SEXPER AS SEXO, CELPER AS NUMERO_TELEFONICO, NOMPLA AS MENU, PREPLA AS PRECIO FROM PERSONA,PLATO; /*Esta vista esta dada por la Persona*/

select * FROM vista_persona;
	